#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Commit mesaj standardı kontrolü (Conventional Commits)
if npx --no-install commitlint --edit "$1"; then
  echo "✅ Commit mesajı doğrulandı."
else
  echo "⛔ Commit mesajı geçersiz! Conventional Commits standardına uymalısınız."
  exit 1
fi
